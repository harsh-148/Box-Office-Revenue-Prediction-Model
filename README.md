# Box-Office-Revenue-Prediction-Model
ML model predicting movie gross revenue from production budget using linear regression — extends prior EDA/correlation analysis into a trained, evaluated model (R² 0.54).
